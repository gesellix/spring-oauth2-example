---
example:
  baseUrl: http://localhost:8080
  oauth2:
    client:
      grantType: client_credentials
      clientId: <client-id>
      clientSecret: <client-secret>
      accessTokenUri: <issuer-uri>/v1/token
spring:
  main:
    banner-mode: "off"
